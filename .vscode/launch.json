{
  "configurations": [
    {
      "name": "(lldb) Launch",
      "type": "lldb",
      "request": "launch",
      "preLaunchTask": "build",
      "program": "${workspaceFolder}/zig-out/bin/dct"
    },
    {
      "name": "(cppdbg-lldb) Launch",
      "type": "cppdbg",
      "request": "launch",
      "preLaunchTask": "build",
      "cwd": "${workspaceFolder}",
      "program": "${workspaceFolder}/zig-out/bin/dct",
      "MIMode": "lldb"
    }
  ]
}